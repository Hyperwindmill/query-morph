{
  "MQL Tagged Template": {
    "prefix": "mql-tagged",
    "body": [
      "const ${1:query} = mql`",
      "  from ${2|json,xml,object|} to ${3|json,xml,object|}",
      "  transform",
      "    set ${4:field} = ${5:value}",
      "    $0",
      "`;"
    ],
    "description": "MQL query with tagged template"
  },
  "MQL Comment Hint": {
    "prefix": "mql-comment",
    "body": [
      "// @mql",
      "const ${1:query} = `",
      "  from ${2|json,xml,object|} to ${3|json,xml,object|}",
      "  transform",
      "    set ${4:field} = ${5:value}",
      "    $0",
      "`;"
    ],
    "description": "MQL query with comment hint"
  },
  "MQL Compile and Execute": {
    "prefix": "mql-compile",
    "body": [
      "const ${1:query} = mql`",
      "  from ${2|json,xml,object|} to ${3|json,xml,object|}",
      "  transform",
      "    set ${4:field} = ${5:value}",
      "`;",
      "",
      "const engine = await compile(${1:query});",
      "const result = engine(${6:sourceData});",
      "$0"
    ],
    "description": "Complete MQL compile and execute pattern"
  }
}
