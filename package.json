{
  "name": "query-morph-monorepo",
  "version": "0.1.3",
  "private": true,
  "description": "Query-Morph Monorepo - A high-performance Query-to-Code engine",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w @query-morph/core",
    "build:all": "npm run build --workspaces --if-present",
    "dev": "npm run dev -w @query-morph/core",
    "test": "npm run test -w @query-morph/core",
    "test:all": "npm run test --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present",
    "playground": "npm run dev -w @query-morph/playground",
    "server": "npm run start:dev -w server"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "prettier": "^3.8.0"
  }
}
